name: Dependency Audit

on:
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: '17 3 * * 1'

permissions:
  contents: read

jobs:
  audit:
    name: Cargo audit and deny
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      # cargo-audit v0.22.2 依赖 kstring 2.0.4 要求 Rust ≥1.96.0，
      # 而 rust-toolchain.toml 固定了 1.95.0。删除 toolchain 文件让 audit
      # 工具使用 runner 自带的最新 stable，不受仓库锁定影响。
      - name: Remove pinned toolchain for audit tools
        run: rm rust-toolchain.toml

      - name: Install Rust stable for audit tools
        uses: dtolnay/rust-toolchain@stable

      - name: RustSec advisory audit
        uses: rustsec/audit-check@69366f33c96575abad1ee0dba8212993eecbe998 # v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Cargo deny checks
        uses: EmbarkStudios/cargo-deny-action@3c6349835b2b7b196a839186cb8b78e02f7b5f25 # v2
