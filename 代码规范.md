# 代码规范 · Zyn iLink ChatBox

> 适用范围：Rust 后端（`src/`）+ 前端 JS（`web/*.js`）+ HTML/CSS（`web/`）。
> 目标：防止低级问题、复制粘贴重复逻辑、死代码/未使用变量。
> 原则基础：ponytail（YAGNI、删 > 改 > 加、根因修复、最短工作 diff）。

---

## 0. 核心原则（不可绕过）

1. **YAGNI first**：没被需求明确要求的代码不要写。 speculative = delete。
2. **删 > 改 > 加**：能删就删，能复用就别新增。
3. **根因修复**：bug 修在共享函数里，不要在每个 caller 加 guard。
4. **最短工作 diff**：理解问题后选最小改动。错误地方的小 diff 是第二个 bug。
5. **复制 3 次必须抽象**：同一逻辑出现 3 处 → 必须抽公共函数。
6. **死代码零容忍**：未使用变量、未调用函数、注释掉的代码块一律删除。
7. **cargo check / node --check 零警告**：警告也算坏代码。

---

## 1. Rust 后端规范

### 1.1 模块与文件

- **职责单一**：一个 `.rs` 文件只负责一个领域（如 `tunnel.rs` 只管隧道、`auth.rs` 只管登录）。跨领域调用走显式 `pub fn`。
- **禁止 `unsafe` 除非绝对必要**：需要时必须配 `// SAFETY:` 注释解释为什么安全。优先用 `Arc<Mutex<T>>` / `once_cell::sync::Lazy` 替代手写 unsafe 全局指针。
- **禁止 `pub` 暴露内部细节**：仅暴露真正被外部调用的项。内部辅助函数用 `fn` 私有。

### 1.2 错误处理

- **信任边界必校验**：HTTP handler 入口、CLI 入口、跨模块调用边界必须校验输入。
- **不要吞错误**：`let _ = ...` 仅用于真正无副作用的清理（如关闭已断开的连接）。其他场景必须 `?` 上抛或 log。
- **错误返回类型统一**：HTTP handler 用 `Result<Json<Value>, Response>`，错误经 `into_response()` 转换。
- **禁止 panic 跨信任边界**：解析外部输入用 `?`，不用 `unwrap()`/`expect()`。

### 1.3 异步与多线程

- **共享状态用 `Arc<Mutex<T>>` 或 `Arc<RwLock<T>>`**：禁止手写 `unsafe static` 全局指针。
- **后台线程固定重连，永不放弃**：reader 线程读到 EOF 后 sleep + 递归重连，与 Python 原版一致。禁止指数退避后放弃重试。
- **stop 标志必须显式**：所有可取消的后台线程必须有 `stop_requested: bool` 字段，stop() 时设置，线程在循环开头检查。
- **thread::spawn 时 Arc::clone 入闭包**：禁止依赖 `unsafe static` 共享状态。

### 1.4 配置与路径

- **所有持久化文件必须走 `config::base_dir()`**：禁止相对路径（`"tunnel_config.json"` 在 CWD=/ 时会写到根目录）。
- **默认值前后端必须同步**：如隧道默认端口 `8888` 在 `tunnel.rs DEFAULT_LOCAL_PORT`、`web.rs default_tunnel_port()` 与前端 `admin.html` 的 `value="8888"` 必须一致；Web 服务默认端口 `8888` 在 `main.rs` / `bot.rs` / `admin.rs TunnelCmd::Start` 中保持一致。
- **env 变量仅用于真正跨部署差异**：单机/服务部署相同的配置不要走 env 分支。
- **可写设置 key 列表单一信息源**：`web.rs is_writable_setting()` 是后端唯一权威，前端 `zn-admin.js _SETTING_DESC_MAP` 必须与之保持一致（修改后端时同步更新前端映射表）。

### 1.5 命名约定

- **类型用 UpperCamelCase**：`TunnelManager`、`AppState`、`AuthUser`。
- **函数/变量用 snake_case**：`spawn_ssh`、`bot_status`、`inner.lock()`。
- **常量用 UPPER_SNAKE_CASE**：`DEFAULT_LOCAL_PORT`、`RECONNECT_INTERVAL_SECS`。
- **私有辅助函数加 `_` 前缀或下划线分隔**：如 `save_config_inner`（私有版本）vs `pub fn save_config`（已删除的包装）。

### 1.6 API 设计

- **同源轮询必须合并端点**：避免 `/tunnel/status` + `/tunnel/logs` 两次请求。合并为单端点返回多字段。
- **批量接口必须存在**：N+1 模式（前端循环调单用户 API）必须改为 `?uids=1,2,3` 批量端点。
- **handler 签名顺序固定**：`Extension<AuthUser>` → `State<Arc<AppState>>` → `Query`/`Json`，便于阅读。
- **路由表与 handler 函数一一对应**：删路由必须同时删 handler，否则 cargo 警告未使用函数。

### 1.7 死代码禁止

- **`cargo check` 警告必须清零**：包括 `unused variable`、`unused import`、`dead_code`、`unused_mut`。
- **删除未调用函数**：私有函数无 caller 立即删。公共函数若仅是"以后可能用"也删（YAGNI）。
- **注释掉的代码块禁止**：用 git 追溯，不要留在文件里。

---

## 2. 前端 JS 规范

### 2.1 模块与全局变量

- **全局变量加 `_` 前缀**：`_admin`、`_listCache`、`_botStatusCache`、`_toast`、`_showConfirm` 等。便于区分项目变量与第三方。
- **不使用 IIFE**：项目采用 `<script src>` 全局加载，所有顶层 `var`/`const` 都是全局变量。
- **跨文件复用必须显式导出到 window**：仅在必要时（如需要 zyn-utils 的 `_escapeHtml` 在 zyn-admin 中可用，因为顶层声明自动全局）。
- **状态对象集中**：`_admin`、`_state`、`_listCache` 是仅有的几个顶层状态对象。新增状态前先看是否能并入现有对象。

### 2.2 WebView 兼容性（强制）

**禁用**以下浏览器原生 API，必须用项目自定义替代：

| 禁用 API | 替代 | 定义位置 |
|---------|------|---------|
| `window.confirm()` | `_showConfirm(msg, onConfirm, onCancel, opts)` | `zn-utils.js` |
| `window.prompt()` | `_showPrompt(msg, defaultValue, onSubmit, opts)` | `zn-utils.js` |
| `window.alert()` | `_showConfirm(msg, fn(){}, null, {okText:"知道了"})` | `zn-utils.js` |
| `console.log` 在生产路径 | `_toast(msg, duration, "error"\|undefined)` | `zn-utils.js` |

原因：WebView / 微信内置浏览器 / Tauri 等环境会静默拦截 `confirm/prompt/alert`，导致管理员操作无响应。

### 2.3 命名空间与去重

- **前端消息去重 key 必须加命名空间前缀**：`'id:123'`、`'row:456'`、`'client:abc'`、`'req:xyz'`、`'media:789'`。
  - 历史教训：`messages` 表的 `id` 和 `messages_v2` 表的 `row_id` 都从 1 自增，共用一个 Set 会冲突，导致中间消息丢失。
- **DOM 查找不可信值必须用属性比较，不用字符串拼接选择器**：用 `_findByDataAttr(attr, value)`（`zn-utils.js`），不要 `document.querySelector('[data-id="' + untrusted + '"]')`。

### 2.4 Toast 使用规范

- **相同文字不重启动画**：`_toast` 内部已用 `isSameText` 判断，文字相同只延长定时器。调用方不要做额外缓存。
- **批量操作只在开始和结束各 toast 一次**：禁止中间进度 toast（idx 变化导致 `isSameText` 永远 false，闪屏）。
- **toast 时长自适应**：调用方不指定时长时，`_toastDuration` 按字数计算（每 30 字 +1s，最少 3s 最多 10s）。error 类型固定最少 5s。
- **错误 toast 必须给用户阅读时间**：error 类型禁止 `< 5000ms`。

### 2.5 事件绑定

- **事件委托优先**：列表型 tbody 用 `addEventListener('click', fn(e) { e.target.closest(...) })`，不要给每行单独绑定。
- **`addEventListener` 替代 `onclick`**：HTML 中不写 `onclick="..."`，统一在 JS 里绑定。原因：XSS 防护 + 代码集中。
- **离开 section 时清理定时器**：如 `_loadSection(name)` 开头检查 `if (name !== "tunnel") _stopTunnelPolling();`，避免后台继续打请求。
- **回调嵌套过深时抽出函数**：超过 2 层嵌套 → 把内层逻辑抽为独立 `var _doXxx = function() {...}`，便于阅读和复用。

### 2.6 复用与抽象

- **复制 3 次必须抽象**：如批量操作 `_bulkUserAction` / `_bulkInviteRevoke` / `_bulkIpUnban` 三份近乎一致的复制粘贴 → 必须抽公共 `_bulkAction(opts)`，仅传 selector / endpoint / reload 函数等差异参数。
- **HTML 渲染辅助函数**：`_loadMoreRowHtml(colspan, remaining)`、`_matchSearch(row, keys, q)` 等已抽到 `zn-admin.js`，复用前先看是否已存在。
- **禁止重复实现 `_escapeHtml` / `_escapeAttr` / `_formatTime` / `_formatBytes`**：这些在 `zn-utils.js` / `zn-admin.js` 已有，直接调用。

### 2.7 死代码禁止

- **未使用变量立即删除**：包括 `_tunnelLogPollId` 这类被合并后不再使用的轮询 ID。
- **被合并的端点对应的前端函数必须删**：如 `/tunnel/logs` 合并到 `/tunnel/status` 后，`_pollTunnelLogs` 函数必须删，不要留作"以后可能用"。
- **`node --check` 必须通过**：提交前对每个 `.js` 跑 `node --check <file>`。
- **注释掉的代码禁止**：git 可追溯。

### 2.8 异步流程

- **Promise 链不要超过 3 层**：超过用 `async/await`（项目用 ES5 风格则抽函数）。
- **`.catch` 必须给用户反馈**：错误经 `_toast(err.message, 3000, "error")`，不要静默。
- **跳转前先 toast 时必须延迟**：`_toast(msg); setTimeout(function() { window.location.href = ...; }, 800);`，因为 `_toast` 内部用 setTimeout 异步渲染，立即跳转会中断显示。

---

## 3. HTML/CSS 规范

### 3.1 样式系统

- **CSS 变量统一**：所有颜色、间距、圆角、字体必须用 `var(--xxx)`，不要硬编码 `#1c1c1e`。变量定义在 `style.css` `:root`。
- **暗色模式自动适配**：通过 `@media (prefers-color-scheme: dark)` 重定义变量，不要写 `.dark .xxx { color: ... }` 这种条件类。
- **modal 系统统一**：所有模态用 `.modal-overlay` + `.modal-box` + `.modal-actions`，或 `.confirm-modal-overlay` + `.confirm-modal`（用于 confirm/prompt 风格）。禁止新建第三套样式。

### 3.2 HTML 结构

- **modal 容器位置**：所有 modal 放在 `<div id="toast">` 之后、`<script>` 之前。便于统一管理。
- **表单字段用 `.modal-field` 容器**：`<label>` + 控件 + 可选 `#xxx-desc` 描述区。描述区用 `display:none` 默认隐藏，JS 填充后显示。
- **列表行 `colspan` 与列数一致**：增删 `<th>` 时同步改 `_listCache.xxx.colspan` 和"加载中..."占位行的 colspan。

### 3.3 布局与响应式

- **关键交互元素必须在视口内**：避免按钮被推到屏幕外。用 `flex-wrap: wrap` + `gap` 让控件自动换行。
- **桌面/移动共用一套样式**：用 `@media (max-width: 768px)` 调整 padding/font-size，不要重写整个布局。
- **iOS 键盘检测阈值**：`visualViewport.height < window.innerHeight * 0.8` 触发键盘弹起处理，避免误报。

---

## 4. 安全规范

### 4.1 XSS 防护

- **不可信值拼入 HTML 必须用 `_escapeHtml`**：用户名、IP、邀请码、审计 detail 等。
- **不可信值拼入 HTML 属性必须用 `_escapeAttr`**：`title="..."`、`data-xxx="..."`、`placeholder="..."`。`_escapeHtml` 不转义引号时不可用。
- **DOM 查找不可信值用属性比较**：`_findByDataAttr(attr, value)`，不用 `querySelector` 字符串拼接。
- **`textContent` 优先**：仅展示文本时用 `el.textContent = ...`，不用 `el.innerHTML = ...`。

### 4.2 敏感信息

- **后端返回设置列表时过滤敏感 key**：`admin_salt`、`server_storage.webdav_password` 等明文密码不返回前端。
- **可写设置 key 白名单**：`is_writable_setting()` 是单一信息源，新增可写 key 必须在此函数加分支。
- **HttpOnly Cookie**：会话 token 走 HttpOnly Cookie，不暴露给 JS。前端 XHR 不需要也不应设置 `X-Session-Token` 头。

### 4.3 IP 封禁校验

- **危险封禁目标必须二次确认**：`0.0.0.0/0`、`::/0`、`127.0.0.0/8`、内网网段（`10.`/`172.16-31.`/`192.168.`/`fc00::/7`）需 confirm。
- **CIDR 前缀范围校验**：IPv4 `/0 ~ /32`，IPv6 `/0 ~ /128`。

---

## 5. 命名规范总表

| 上下文 | 风格 | 示例 |
|-------|------|------|
| Rust 类型 | UpperCamelCase | `TunnelManager` |
| Rust 函数/变量 | snake_case | `spawn_ssh` |
| Rust 常量 | UPPER_SNAKE_CASE | `DEFAULT_LOCAL_PORT` |
| Rust 私有辅助 | `_inner` 后缀或 `_` 前缀 | `save_config_inner` |
| JS 全局变量/函数 | `_lowerCamelCase` | `_admin`、`_showConfirm` |
| JS 常量 | `_UPPER_SNAKE_CASE` | `_SETTING_DESC_MAP` |
| JS DOM 元素 ID | `kebab-case` | `setting-edit-desc-wrap` |
| JS data 属性 | `data-kebab-case` | `data-action`、`data-uid` |
| CSS 类 | `kebab-case` | `confirm-modal-overlay` |
| CSS 变量 | `--kebab-case` | `--bg-secondary`、`--accent` |
| Rust 文件名 | snake_case | `bot_manager.rs` |
| JS 文件名 | `zn-kebab-case.js` 或 `zn-xxx.js` | `zn-admin.js`、`zn-utils.js` |

---

## 6. 文件位置规范

| 类型 | 位置 | 命名 |
|------|------|------|
| Rust 源码 | `src/*.rs` | snake_case |
| 前端 JS | `web/*.js` | `zn-xxx.js`（聊天端）/ `landing.js`（登录页） |
| 前端 HTML | `web/*.html` | snake_case |
| 全局样式 | `web/style.css` | 单一文件 |
| 持久化数据 | `config::base_dir()` 返回目录 | 文件名 snake_case |
| 审计/迁移文件 | 项目根 `*.md` | `YYYY-MM-DD` 后缀 |
| 项目文档 | 项目根 `README.md`、`代码规范.md`、`SECURITY-HISTORY.md` | 固定名 |
| 云端用户文档 | 由 `docs.url` 系统设置指向 | 任意外链 |

**禁止**：将数据写到 `%APPDATA%`（除非用户在设置中显式切换）。默认便携模式，数据放程序目录。

---

## 7. 提交前检查清单

### Rust 后端
- [ ] `cargo check` 零警告零错误
- [ ] `cargo clippy`（若安装）无新警告
- [ ] 无 `unwrap()`/`expect()` 在信任边界
- [ ] 无 `unsafe` 块（除非配 `// SAFETY:` 注释）
- [ ] 无未使用 `pub fn`（删路由同时删 handler）
- [ ] 所有持久化路径走 `config::base_dir()`
- [ ] 默认值前后端同步

### 前端 JS
- [ ] `node --check` 对每个修改的 `.js` 文件通过
- [ ] 无 `window.confirm` / `window.prompt` / `window.alert`
- [ ] 无 `console.log` 在生产路径
- [ ] 无 `onclick="..."` 内联事件
- [ ] 无复制 3 次以上的逻辑（已抽象为公共函数）
- [ ] 无注释掉的代码块
- [ ] 无未使用的顶层变量
- [ ] toast 时长合理（error ≥ 5s）
- [ ] 异步错误都有 `.catch` + `_toast`
- [ ] 跳转前的 toast 用 `setTimeout` 延迟 800ms

### HTML/CSS
- [ ] 无硬编码颜色（用 `var(--xxx)`）
- [ ] 增删列时 colspan 同步
- [ ] modal 用现有 `.modal-overlay` / `.confirm-modal-overlay` 样式
- [ ] 关键按钮在视口内（缩放 + 长内容 + iOS 键盘弹起均可见可点）

### 安全
- [ ] 不可信值拼入 HTML 用 `_escapeHtml`
- [ ] 不可信值拼入属性用 `_escapeAttr`
- [ ] IP 封禁危险值二次确认
- [ ] 敏感设置 key 不返回前端

---

## 8. 常见反模式（禁止）

| 反模式 | 后果 | 正确做法 |
|--------|------|---------|
| 复制粘贴 3 处一致逻辑 | 改一处忘改另两处 | 抽公共函数 |
| `unsafe static` 全局指针 | 初始化时机错乱，UB | `Arc<Mutex<T>>` |
| `window.confirm` 在 WebView | 静默拦截，操作无响应 | `_showConfirm` |
| 前端消息去重用裸 ID | 跨表 ID 冲突，消息丢失 | 加命名空间前缀 `'id:123'` |
| 相对路径配置文件 | CWD=/ 时写到根目录 | `config::base_dir().join(...)` |
| `/status` + `/logs` 双端点 | 双倍请求量 | 合并为单端点 |
| N+1 单用户 API 调用 | 列表 100 用户 = 100 请求 | 批量端点 `?uids=...` |
| `idx % 5 === 0` 进度 toast | idx 变化导致闪屏 | 只开始/结束各一次 |
| 注释掉的代码 | 误导后续维护者 | 删除，git 可追溯 |
| `cargo check` 警告忽略 | 死代码堆积 | 立即清理 |
| 默认值前后端不一致 | 用户改不了或改了无效 | 单一信息源 + 同步检查 |
| 信任边界 `unwrap()` | panic 整个进程 | `?` 上抛错误 |
| 可写 key 未在白名单 | 入侵 admin 可改任意设置 | `is_writable_setting()` 白名单 |

---

## 9. 修订记录

| 日期 | 版本 | 内容 |
|------|------|------|
| 2026-07-21 | v1.0 | 首版。基于隧道与管理面板审计修复总结。 |
| 2026-07-21 | v1.1 | 校正与实际代码对齐：① 默认端口由 `1145` 修正为 `8888`（与 `tunnel.rs DEFAULT_LOCAL_PORT`、`web.rs default_tunnel_port()`、`admin.html value="8888"` 一致）；② 补充 `default_tunnel_port` 实际定义于 `web.rs`；③ 删除已不存在的 `SKILL.md` 引用，原则基础改为 ponytail；④ 文件位置表新增 `README.md`、`SECURITY-HISTORY.md` 与云端用户文档（`docs.url`）说明。 |
