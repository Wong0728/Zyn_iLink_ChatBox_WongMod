# iLink-WM1 部署指南

> 本文档详细介绍 iLink-WM1 在 Linux 与 Windows 环境下的各种部署方式，从个人单机到生产环境全覆盖。读完本文档，你可以把整套系统在任意平台上跑起来。

**版本**：v1.0 · 2026-07-21
**适用版本**：iLink-WM1 v3.1.9-wm1.0 及以上

---

## 目录

- [一、环境要求总览](#一环境要求总览)
- [二、Linux 部署](#二linux-部署)
  - [2.1 一键脚本 install.sh（推荐）](#21-一键脚本-installsh推荐)
  - [2.2 手动部署（Rust 编译 + systemd）](#22-手动部署rust-编译--systemd)
  - [2.3 Nginx + HTTPS 反向代理](#23-nginx--https-反向代理)
- [三、Windows 部署](#三windows-部署)
  - [3.1 版本 A · 双击 exe 即用（个人单机）](#31-版本-a--双击-exe-即用个人单机)
  - [3.2 版本 B · Windows Server + 宝塔面板](#32-版本-b--windows-server--宝塔面板)
  - [3.3 版本 C · Windows 服务（NSSM 守护）](#33-版本-c--windows-服务nssm-守护)
- [四、首次启动向导](#四首次启动向导)
- [五、云端文档与用户协议配置](#五云端文档与用户协议配置)
- [六、升级与备份](#六升级与备份)
- [七、故障排查](#七故障排查)

---

## 一、环境要求总览

| 项 | Linux | Windows |
|----|-------|---------|
| 操作系统 | Ubuntu 20.04+ / Debian 11+ / CentOS 8+ / Rocky / Alpine | Windows 10 / 11 / Server 2016+ |
| 架构 | x86_64 / aarch64 | x86_64 |
| 内存 | ≥ 512 MB（编译需 1GB+，可加 swap） | ≥ 512 MB（编译需 1GB+） |
| 磁盘 | ≥ 1 GB | ≥ 1 GB |
| 端口 | 8888/tcp（默认，可改） | 8888/tcp |
| 浏览器 | Chrome 90+ / Edge 90+ / Firefox 88+ | 同左 |
| 网络 | 服务端需访问微信 iLink 域名 | 同左 |

---

## 二、Linux 部署

### 2.1 一键脚本 install.sh（推荐）

#### 2.1.1 上传源码包到服务器

在 Windows 本机用 PowerShell：

```powershell
scp "C:\...\ilink_wm_v3.2.3\ilink_wm_v3.2.3_src.zip" ubuntu@1.2.3.4:/tmp/
# 同时上传 install.sh
scp "C:\...\ilink_wm_v3.2.3\install.sh" ubuntu@1.2.3.4:/tmp/
```

也可用 WinSCP / FileZilla / 宝塔面板文件管理上传。

#### 2.1.2 执行一键安装

SSH 登录服务器后：

```bash
# 赋予执行权限
chmod +x /tmp/install.sh

# 一键安装（自动检测系统、装依赖、编译、配 systemd）
sudo bash /tmp/install.sh /tmp/ilink_wm_v3.2.3_src.zip

# 不带参数（默认从 /tmp/ilink_wm_v3.2.3_src.zip 读取）
sudo bash /tmp/install.sh
```

脚本会自动完成：

1. 检测包管理器（apt / dnf / yum）
2. 安装依赖：构建工具、OpenSSL、ffmpeg、OpenSSH client、unzip、curl
3. 检查预先安装并由管理员验证来源的 Rust stable 工具链
4. 创建 `/opt/ilink` 目录与 `ilink` 系统用户
5. 解压源码到 `/opt/ilink/ilink_wm_v3.2.3`
6. `cargo build --release`（显示进度，约 3-10 分钟）
7. 创建数据目录并在终端执行 `admin init` 创建 owner
8. 明确选择“已有 HTTPS 反代”或“受信内网明文”模式
9. 生成 `/etc/ilink/env` 与 `/etc/systemd/system/ilink.service`
10. `systemctl enable --now ilink` 并配置防火墙

#### 2.1.3 验证安装

```bash
# 服务状态
sudo systemctl status ilink

# 实时日志
sudo journalctl -u ilink -f

# 端口监听
ss -tlnp | grep 8888
```

owner 已在安装过程中创建。启动后通过所选的安全入口登录，再扫码绑定 iLink；没有浏览器安装向导。

#### 2.1.4 卸载

```bash
sudo systemctl stop ilink
sudo systemctl disable ilink
sudo rm /etc/systemd/system/ilink.service
sudo rm -rf /opt/ilink /etc/ilink
sudo userdel ilink
```

---

### 2.2 手动部署（Rust 编译 + systemd）

适合想完全掌控每一步的用户。

#### 2.2.1 安装 Rust 工具链

按照 <https://www.rust-lang.org/tools/install> 的官方说明安装 stable 工具链，并先核验下载来源/签名，再执行安装程序。部署脚本不会把远程响应直接通过管道交给 shell。安装后运行 `cargo --version` 验证。

#### 2.2.2 安装系统依赖

```bash
# Debian / Ubuntu
sudo apt update
sudo apt install -y build-essential pkg-config libssl-dev unzip ffmpeg openssh-client

# CentOS / RHEL / Rocky
sudo dnf groupinstall -y "Development Tools"
sudo dnf install -y openssl-devel pkg-config unzip ffmpeg openssh-clients
```

#### 2.2.3 解压源码

```bash
sudo mkdir -p /opt/ilink
sudo chown $USER:$USER /opt/ilink
cd /opt/ilink
unzip /tmp/ilink_wm_v3.2.3_src.zip
ls ilink_wm_v3.2.3/  # 应看到 src/ web/ Cargo.toml 等
```

#### 2.2.4 编译

```bash
cd /opt/ilink/ilink_wm_v3.2.3
cargo build --release
ls -lh target/release/ilink-wm1  # 编译产物
```

> 内存不足导致 OOM？加 swap：
> ```bash
> sudo fallocate -l 2G /swapfile && sudo chmod 600 /swapfile
> sudo mkswap /swapfile && sudo swapon /swapfile
> echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab
> ```

#### 2.2.5 配置系统用户与目录权限

```bash
sudo useradd -r -s /usr/sbin/nologin -d /opt/ilink ilink
sudo chown -R ilink:ilink /opt/ilink
```

#### 2.2.6 配置环境变量

```bash
sudo mkdir -p /etc/ilink
sudo tee /etc/ilink/env >/dev/null <<'EOF'
ILINK_HOST=0.0.0.0
ILINK_PORT=8888
ILINK_DATA_DIR=/opt/ilink/data
RUST_LOG=ilink_wm1=info
RUST_BACKTRACE=full
EOF
sudo chown root:ilink /etc/ilink/env
sudo chmod 640 /etc/ilink/env
```

#### 2.2.7 创建 systemd 服务

```bash
sudo tee /etc/systemd/system/ilink.service >/dev/null <<'EOF'
[Unit]
Description=iLink-WM1 ChatBox Service
After=network.target

[Service]
Type=simple
User=ilink
Group=ilink
WorkingDirectory=/opt/ilink/ilink_wm_v3.2.3
EnvironmentFile=/etc/ilink/env
ExecStart=/opt/ilink/ilink_wm_v3.2.3/target/release/ilink-wm1
Restart=on-failure
RestartSec=5s
LimitNOFILE=65536

NoNewPrivileges=true
ProtectSystem=strict
ProtectHome=true
PrivateTmp=true
ReadWritePaths=/opt/ilink

[Install]
WantedBy=multi-user.target
EOF

sudo systemctl daemon-reload
sudo systemctl enable --now ilink
sudo systemctl status ilink
```

#### 2.2.8 开放防火墙

```bash
# Ubuntu / Debian (ufw)
sudo ufw allow 8888/tcp
sudo ufw reload

# CentOS / RHEL (firewalld)
sudo firewall-cmd --permanent --add-port=8888/tcp
sudo firewall-cmd --reload
```

云服务器还需在控制台安全组放行 8888 端口。

---

### 2.3 Nginx + HTTPS 反向代理

强烈推荐生产环境使用，理由：
- 启用 HTTPS 加密
- 隐藏后端端口
- 满足公网部署守卫（可信代理 + 上游真实 TLS）

#### 2.3.1 安装 Nginx 与 Certbot

```bash
sudo apt install -y nginx certbot python3-certbot-nginx
# 或
sudo dnf install -y nginx certbot python3-certbot-nginx
```

#### 2.3.2 准备域名

把 `chat.example.com`（替换成你的域名）A 记录指向服务器 IP。

#### 2.3.3 创建 Nginx 配置

```bash
sudo tee /etc/nginx/sites-available/ilink >/dev/null <<'EOF'
server {
    listen 80;
    server_name chat.example.com;

    client_max_body_size 50m;

    location / {
        proxy_pass http://127.0.0.1:8888;
        proxy_http_version 1.1;

        # WebSocket 升级（聊天实时通信必需）
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # 透传客户端真实 IP
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
    }
}
EOF

sudo ln -sf /etc/nginx/sites-available/ilink /etc/nginx/sites-enabled/
sudo nginx -t && sudo systemctl reload nginx
```

#### 2.3.4 申请 HTTPS 证书

```bash
sudo certbot --nginx -d chat.example.com --redirect --agree-tos -m your@email.com
```

certbot 会自动：
- 申请 Let's Encrypt 证书
- 修改 Nginx 配置加 443 监听
- 添加强制 HTTPS 跳转
- 装好定时续签

#### 2.3.5 通知后端走 HTTPS（公网部署守卫）

```bash
sudo tee -a /etc/ilink/env >/dev/null <<'EOF'

# 公网部署守卫
ILINK_TRUSTED_PROXIES=127.0.0.1
ILINK_FORCE_HTTPS=true
EOF

sudo systemctl restart ilink
sudo journalctl -u ilink -n 50 --no-pager
```

此时访问 `https://chat.example.com` 即可。

> `ILINK_FORCE_HTTPS` 只设置 Secure Cookie、HSTS 等 HTTPS 语义，不会给 HTTP 连接增加 TLS；必须先由 Nginx/宝塔/Caddy 等上游真正终止 HTTPS。

---

## 三、Windows 部署

提供三种版本，按场景选择：

| 版本 | 场景 | 推荐人群 |
|------|------|---------|
| A · 双击 exe | 个人单机使用 | 个人用户、临时测试 |
| B · 宝塔面板 | Windows Server + 宝塔 | 已用宝塔的运维 |
| C · NSSM 服务 | Windows Server 生产 | 不用宝塔的企业 |

### 3.1 版本 A · 双击 exe 即用（个人单机）

适合个人/小团队自用，下载即用，关机即停。

#### 3.1.1 获取分发包

下载 `ilink_wm_v3.2.3_win_x64.zip` 到本机。

#### 3.1.2 解压

解压到任意目录，例如 `D:\ilink-wm1\`，结构如下：

```
D:\ilink-wm1\
├── ilink-wm1.exe
├── web/
├── start.ps1
├── install-service.ps1
├── README.md
├── 用户协议.md
└── 部署指南.md
```

#### 3.1.3 启动

**右键 `start.ps1` →「使用 PowerShell 运行」**（或在 PowerShell 中执行 `.\start.ps1`），脚本会：

1. 启动 `ilink-wm1.exe`
2. 等待端口 8888 就绪
3. 自动打开浏览器访问 `http://localhost:8888`
4. 显示控制台窗口（实时日志），关闭窗口即停止服务

> 注：`start.ps1` / `install-service.ps1` 为 PowerShell 脚本（中文提示）。项目已全面弃用 cmd 批处理——cmd.exe 无法可靠解析含中文的 .bat/.cmd。若提示执行策略受限，先执行：
> `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned`

首次启动会进入交互式向导（见第四章），按提示完成 owner 账号创建。

#### 3.1.4 关闭

直接关闭弹出的控制台窗口，或按 `Ctrl+C`。

#### 3.1.5 数据位置

默认在 `D:\ilink-wm1\data\`，可通过环境变量 `ILINK_DATA_DIR` 修改。

#### 3.1.6 开机自启（可选）

把 `start.ps1` 的快捷方式放到 `shell:startup`（快捷方式目标建议写：
`powershell.exe -ExecutionPolicy Bypass -File "D:\ilink-wm1\start.ps1"`）：

```powershell
# 打开启动文件夹
explorer shell:startup

# 把 start.ps1 的快捷方式拖进去
```

或用版本 C 的 NSSM 方案做成 Windows 服务。

---

### 3.2 版本 B · Windows Server + 宝塔面板

适合已经用宝塔面板管理 Windows Server 的用户。

#### 3.2.1 安装宝塔面板

在 Windows Server 上访问 <https://www.bt.cn/new/download.html> 下载 Windows 版宝塔，按提示安装。

#### 3.2.2 上传分发包

1. 下载 `ilink_wm_v3.2.3_win_x64.zip`
2. 登录宝塔面板 → 文件 → 上传到 `D:\ilink-wm1\`
3. 在宝塔面板内右键解压

#### 3.2.3 用 NSSM 守护进程

宝塔 Windows 版自带"进程管理"插件，但更推荐用 NSSM（更稳定）：

1. 下载固定版本 NSSM：<https://nssm.cc/release/nssm-2.24.zip>，并校验 SHA-256：`727D1E42275C605E0F04ABA98095C38A8E1E46DEF453CDFFCE42869428AA6743`
2. 解压 `nssm.exe`（64 位版本）到 `D:\ilink-wm1\bin\`
3. 以管理员身份打开 PowerShell：

```powershell
cd D:\ilink-wm1
.\bin\nssm.exe install ilink-wm1 "D:\ilink-wm1\ilink-wm1.exe"
.\bin\nssm.exe set ilink-wm1 AppDirectory "D:\ilink-wm1"
.\ilink-wm1.exe admin init
.\bin\nssm.exe set ilink-wm1 AppEnvironmentExtra "ILINK_HOST=127.0.0.1" "ILINK_PORT=8888" "ILINK_SERVER_MODE=1" "ILINK_DATA_DIR=D:\ilink-wm1\data" "RUST_LOG=ilink_wm1=info"
.\bin\nssm.exe set ilink-wm1 Start SERVICE_AUTO_START
.\bin\nssm.exe start ilink-wm1
```

服务即以后台方式运行，开机自启、崩溃自动重启。

#### 3.2.4 在宝塔创建网站做反代

1. 宝塔面板 → 网站 → 添加站点
2. 域名：`chat.example.com`
3. 类型：HTML（仅用作反代入口）
4. 创建后 → 设置 → 反向代理 → 添加反向代理：
   - 代理名称：`ilink`
   - 目标URL：`http://127.0.0.1:8888`
   - 发送域名：`$host`

5. 申请 SSL：网站设置 → SSL → Let's Encrypt（免费）→ 强制 HTTPS

#### 3.2.5 Nginx 配置（手动方式）

如不用宝塔反代功能，手动配置宝塔的 Nginx：

```nginx
# 宝塔 Nginx 站点配置
server {
    listen 80;
    server_name chat.example.com;
    client_max_body_size 50m;

    location / {
        proxy_pass http://127.0.0.1:8888;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
    }
}
```

#### 3.2.6 通知后端走 HTTPS

修改 NSSM 服务环境变量：

```powershell
cd D:\ilink-wm1
.\bin\nssm.exe set ilink-wm1 AppEnvironmentExtra "ILINK_HOST=127.0.0.1" "ILINK_PORT=8888" "ILINK_DATA_DIR=D:\ilink-wm1\data" "ILINK_SERVER_MODE=1" "RUST_LOG=ilink_wm1=info" "ILINK_TRUSTED_PROXIES=127.0.0.1" "ILINK_FORCE_HTTPS=1"
.\bin\nssm.exe restart ilink-wm1
```

访问 `https://chat.example.com` 即可。

---

### 3.3 版本 C · Windows 服务（NSSM 守护）

适合 Windows Server 生产环境，不用宝塔。

#### 3.3.1 解压分发包

解压 `ilink_wm_v3.2.3_win_x64.zip` 到 `D:\ilink-wm1\`。

#### 3.3.2 运行 install-service.ps1

管理员 PowerShell 中执行 `.\install-service.ps1`（权限不足会自动弹 UAC 提权），或直接用 `iLinkWM install-service`。脚本会：

1. 下载固定版本 NSSM 并校验 SHA-256
2. 创建数据目录，在终端完成 owner 初始化
3. 明确选择已有 HTTPS 反代或受信内网明文模式
4. 写入 `ILINK_SERVER_MODE=1` 后注册并启动服务

#### 3.3.3 服务管理

```powershell
# 查看服务状态
Get-Service ilink-wm1

# 启动 / 停止 / 重启
Start-Service ilink-wm1
Stop-Service ilink-wm1
Restart-Service ilink-wm1

# 查看日志（服务输出重定向到 logs\service.log）
Get-Content D:\ilink-wm1\logs\service.log -Tail 100 -Wait
```

#### 3.3.4 卸载服务

```powershell
cd D:\ilink-wm1
.\bin\nssm.exe stop ilink-wm1
.\bin\nssm.exe remove ilink-wm1 confirm
```

#### 3.3.5 配置 Nginx 反代（同 3.2.4）

Windows 上也可装 Nginx 做反代，下载：<https://nginx.org/en/download.html>

配置文件位置 `conf\nginx.conf`，内容同 3.2.5。配置后：

```powershell
cd C:\nginx
.\nginx.exe -t
.\nginx.exe -s reload
```

---

## 四、首次初始化

Linux/Windows 服务安装器会在注册服务前于终端执行 `ilink-wm1 admin init`。直接前台运行二进制时，仍可进入交互式首次初始化流程。

### 4.1 绑定地址

- 服务安装器默认 `0.0.0.0`（监听全部 IPv4 网卡，并要求选择安全模式）
- 仅本机测试填 `127.0.0.1`
- 走反代填 `127.0.0.1`，由 Nginx 处理公网

### 4.2 端口

默认 `8888`，可改成任意未占用端口。

### 4.3 数据目录

未设置 `ILINK_DATA_DIR` 时默认使用可执行文件所在目录；服务部署建议显式设置绝对目录：
- Linux：`/opt/ilink/data`
- Windows：`D:\ilink-wm1\data`

### 4.4 owner 用户名

管理员账号，例如 `admin`。创建后不可改。

### 4.5 owner 密码

8-128 位，必须含大小写字母 + 数字，避开弱口令黑名单。

### 4.6 是否启用隧道

- 选 N：生产环境用反向代理，不用 serveo
- 选 Y：测试或无公网 IP 时用 serveo.net 内网穿透

### 4.7 是否开放注册

- 建议选 N：用邀请码注册更安全
- 选 Y：任何人可直接注册（不推荐）

初始化完成后服务启动。通过 HTTPS 反代地址或已确认的受信内网 HTTP 地址打开首页，用 owner 账号登录。

---

## 五、云端文档与用户协议配置

前端首页"文档"和"使用守则"按钮默认隐藏，需要把文档上传到云端后配置外链。

### 5.1 上传文档

把项目根目录的两份文档上传到云端：

- `README.md` → 用户使用文档（docs.url）
- `用户协议.md` → 用户协议（terms.url）

**推荐平台**：
- GitHub Gist（免费、国外访问快）
- 飞书云文档（国内访问快、支持协同）
- 自建静态站点（直接放服务器上由 Nginx 提供）

### 5.2 GitHub Gist 步骤

1. 访问 <https://gist.github.com>
2. 文件名填 `README.md`，粘贴文档全文，Create public gist
3. 点 Raw 按钮，复制 URL，形如：
   ```
   https://gist.githubusercontent.com/yourname/abc123/raw/README.md
   ```
4. `用户协议.md` 同样操作

### 5.3 飞书云文档步骤

1. 访问 <https://www.feishu.cn>，登录账号
2. 新建文档 → 导入 Markdown → 选 `README.md`
3. 发布为公开链接（任何人可访问）
4. 复制链接，形如：
   ```
   https://xxx.feishu.cn/docx/xxxxxxxxxxxxxxxx
   ```
5. `用户协议.md` 同样操作

### 5.4 自建静态站点（推荐生产环境）

```bash
# Linux：Nginx 直接放
sudo mkdir -p /var/www/ilink/docs
sudo cp /opt/ilink/ilink_wm_v3.2.3/README.md /var/www/ilink/docs/
sudo cp /opt/ilink/ilink_wm_v3.2.3/用户协议.md /var/www/ilink/docs/

# Nginx 站点配置加 location
# 已有 server { } 块内追加：
# location /docs/ {
#     alias /var/www/ilink/docs/;
#     default_type text/markdown;
#     charset utf-8;
# }
sudo nginx -t && sudo systemctl reload nginx
# URL 即为 https://chat.example.com/docs/README.md
#         https://chat.example.com/docs/用户协议.md
```

### 5.5 通知后端外链地址

```bash
# Linux CLI
cd /opt/ilink/ilink_wm_v3.2.3
./target/release/ilink-wm1 admin config set docs.url "https://chat.example.com/docs/README.md"
./target/release/ilink-wm1 admin config set terms.url "https://chat.example.com/docs/用户协议.md"

# 或 Web 管理面板
# 登录 https://chat.example.com/admin
# → 系统设置 → 找到 docs.url（标签"文档链接"）→ 编辑 → 粘贴 URL → 保存
# → 找到 terms.url（标签"使用守则链接"）→ 编辑 → 粘贴 URL → 保存
```

刷新首页，"文档"与"使用守则"按钮即出现并指向对应链接。

### 5.6 同步 terms_version（重要）

更新用户协议内容后，需同步升版本号触发已同意旧版用户重新确认：

```bash
./target/release/ilink-wm1 admin config set terms_version "1.1"
```

---

## 六、升级与备份

### 6.1 备份

#### 6.1.1 Linux

```bash
# 一键备份
sudo systemctl stop ilink
sudo tar -czf /opt/ilink/backup-$(date +%Y%m%d).tar.gz -C /opt/ilink data
sudo systemctl start ilink

# 自动备份（每日凌晨 3 点）
echo '0 3 * * * systemctl stop ilink && tar -czf /opt/ilink/backup-$(date +\%Y\%m\%d).tar.gz -C /opt/ilink data && systemctl start ilink && find /opt/ilink -name "backup-*.tar.gz" -mtime +14 -delete' | sudo crontab -
```

#### 6.1.2 Windows

```powershell
# 手动备份
Stop-Service ilink-wm1
Compress-Archive -Path D:\ilink-wm1\data -DestinationPath "D:\ilink-wm1\backup-$(Get-Date -Format 'yyyyMMdd').zip" -Force
Start-Service ilink-wm1

# 自动备份（用任务计划程序）
# schtasks /create /tn "iLink Backup" /tr "powershell -Command Stop-Service ilink-wm1; Compress-Archive -Path D:\ilink-wm1\data -DestinationPath ('D:\ilink-wm1\backup-' + (Get-Date -Format yyyyMMdd) + '.zip') -Force; Start-Service ilink-wm1" /sc daily /st 03:00 /ru SYSTEM
```

### 6.2 升级

```bash
# Linux
# 1. 备份（见 6.1.1）
# 2. 上传新源码包覆盖
cd /opt/ilink/ilink_wm_v3.2.3
unzip -o /tmp/ilink_wm_v3.2.3_src.zip

# 3. 重新编译
cargo build --release

# 4. 重启
sudo systemctl restart ilink

# 5. 验证
sudo systemctl status ilink
sudo journalctl -u ilink -n 50 --no-pager
```

```powershell
# Windows
# 1. 备份（见 6.1.2）
# 2. 上传新分发包覆盖（保留 data/ 目录）
# 3. 重启服务
Restart-Service ilink-wm1

# 4. 验证
Get-Service ilink-wm1
Get-Content D:\ilink-wm1\logs\service.log -Tail 50
```

### 6.3 回滚

```bash
# Linux
sudo systemctl stop ilink
# 用旧二进制替换 target/release/ilink-wm1
# 从备份恢复 data/ 目录
sudo tar -xzf /opt/ilink/backup-20260720.tar.gz -C /opt/ilink
sudo systemctl start ilink
```

```powershell
# Windows
Stop-Service ilink-wm1
# 用旧 exe 替换
# 解压旧备份到 D:\ilink-wm1\data
Expand-Archive -Path "D:\ilink-wm1\backup-20260720.zip" -DestinationPath D:\ilink-wm1 -Force
Start-Service ilink-wm1
```

---

## 七、故障排查

### 7.1 常见问题速查

| 现象 | 排查 |
|------|------|
| 浏览器打不开 | `systemctl status ilink` / `Get-Service ilink-wm1` 看服务；`ss -tlnp` / `netstat -ano` 看端口；云控制台安全组放行 8888 |
| 启动报公网部署守卫错误 | 上游已提供真实 HTTPS 时同时设置 `ILINK_TRUSTED_PROXIES=127.0.0.1` 与 `ILINK_FORCE_HTTPS=1`；仅受信内网可显式设置 `ILINK_ALLOW_INSECURE_PUBLIC=1` |
| 编译 OOM | Linux 加 swap；Windows 关闭其他大程序 |
| 编译报 openssl 找不到 | `apt install libssl-dev pkg-config`（Debian/Ubuntu） |
| WebSocket 连不上、聊天不实时 | Nginx 没加 `Upgrade`/`Connection` 头 |
| 上传大文件失败 | Nginx `client_max_body_size` 太小；后端默认上限 50MB |
| 邀请码无法注册 | 检查 `allow_invite_registration=true`，邀请码是否过期 |
| "文档"按钮不显示 | 未配置 `docs.url`（见第五章） |
| 改密码后立即掉线 | 正常（H-12 安全机制），改密即清全部 session 与 device_token |
| CLI 二次身份确认失败 | 本机执行设 `ILINK_CLI_TRUST=1` 临时跳过 |

### 7.2 日志查看

```bash
# Linux systemd
sudo journalctl -u ilink -f                    # 实时
sudo journalctl -u ilink -n 1000 --no-pager    # 最近 1000 行
sudo journalctl -u ilink --since "2026-07-21 00:00:00" --until "2026-07-21 23:59:59"
```

```powershell
# Windows NSSM 服务
Get-Content D:\ilink-wm1\logs\service.log -Tail 100 -Wait
```

### 7.3 查看详细错误

```bash
# 临时开启 debug 日志
sudo systemctl stop ilink
sudo ILINK_LOG=ilink_wm1=debug /opt/ilink/ilink_wm_v3.2.3/target/release/ilink-wm1
# Ctrl+C 停止后重新启动服务
sudo systemctl start ilink
```

### 7.4 数据库检查

```bash
# Linux（需安装 sqlite3）
sudo apt install -y sqlite3
sudo sqlite3 /opt/ilink/data/system.db ".tables"
sudo sqlite3 /opt/ilink/data/system.db "SELECT key, value FROM system_settings WHERE key LIKE '%docs%' OR key LIKE '%terms%';"

# 查看用户列表
sudo sqlite3 /opt/ilink/data/system.db "SELECT uid, username, role, status FROM users;"
```

### 7.5 重置 owner 密码

忘记 owner 密码时：

```bash
# 在二进制所在机器上执行
cd /opt/ilink/ilink_wm_v3.2.3
sudo systemctl stop ilink
# S10 二次身份确认会要求输入 owner/admin 凭据；此时无 admin 可用，
# 临时设 ILINK_CLI_TRUST=1 跳过（仅限受控本机！）
sudo ILINK_CLI_TRUST=1 ./target/release/ilink-wm1 admin user reset-password <owner_username>
# 按提示输入新密码
sudo systemctl start ilink
```

### 7.6 联系支持

- **原项目**：ZynSync · <https://github.com/zynsync/Zyn-iLink-ChatBox>
- **Rust 移植**：Mr.Wong
- **完整文档**：见 `README.md`
- **安全历史**：见 `SECURITY-HISTORY.md`

---

*本部署指南最后更新于 2026-07-21。*
